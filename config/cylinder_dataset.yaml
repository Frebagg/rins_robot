# Ultralytics YOLO instance segmentation dataset for RINS cylinders.
# Run training from the rins_robot package root so this relative path resolves.
path: datasets/cylinder_yolo
train: images/train
val: images/val

names:
  0: cylinder_upright
  1: cylinder_lying
  2: liquid_puddle
